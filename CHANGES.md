# FAKE CONTACT 更新メモ（現実味を高める4点）

## 1. 動画が実際に動く

`components/game/media-player.tsx` を新規追加しました。

- 再生ボタンを押すと時間が進み、再生バーが伸び、終わると「もう一度」に変わります。
- **動画ファイルがまだ無くても動きます。**ファイルが無い場合は、これまでの静止画をゆっくり寄せながら「再生している風」に見せます（体験の流れは同じ）。
- ディープフェイク回だけ、映像に走査線のようなノイズがときどき走ります。観察すれば気づける程度の控えめな強さにしてあります。

### 本物の動画に差し替えるとき

1. 動画ファイルを `public/content/` に置く（例: `public/content/deepfake.mp4`）
2. `lib/scenarios.ts` の `s6_deepfake` を書き換える

```ts
video: undefined,          // ← これを
video: "/content/deepfake.mp4",  // ← こう変える
```

形式は **MP4（H.264）** が最も確実です。`playsInline` を付けてあるので、iPhone / iPad でも勝手に全画面にならず、SNSアプリらしい見た目のまま再生されます。

なお動画は最初は消音で始まります。音付きの自動再生はスマホのブラウザが必ずブロックするためで、再生バー右のスピーカーマークで音を出せます。

---

## 2. 再チャレンジで7つの順番がランダムになる

`lib/shuffle.ts` を新規追加し、`fake-contact-game.tsx` の出題順を差し替えました。

- フィッシャー・イェーツ法という、どの並びも等確率で出る偏りのないシャッフルを使っています。
- **前回の1問目と同じ問題からは始まりません。**「またこれか」を防ぐためです。
- 1回目から固定順にしたい場合は、`lib/shuffle.ts` の `SHUFFLE_FIRST_PLAY` を `false` にしてください。

シャッフルは「訓練をはじめる」を押した瞬間に行っています。ページを組み立てる段階でランダムにすると、サーバー側で作った画面とブラウザ側の画面が食い違い、React が警告を出すためです。

---

## 3. ボタンを押すと音が鳴る

`lib/sfx.ts` を新規追加しました。**音声ファイルは一切使わず、その場で波形を合成しています。**追加ファイル不要・読み込み待ちゼロで、GitHub Pages でもそのまま鳴ります。

- 3つの選択肢は音の高さが違います（高い＝信頼／中＝怪しい／低い＝ブロック）。手元を見ていなくても押した先が分かります。
- 判定音は、ボタン音と重ならないよう 0.22 秒遅らせて鳴らします。
- 開始画面の右上と、プレイ中の右上に**消音ボタン**があります。設定は次回も引き継がれます。
- iPhone / iPad は「画面に触れるまで音を鳴らせない」制限があるため、最初のタップで音を解禁する処理（`unlockAudio()`）を入れてあります。

---

## 4. リンクにカーソルを乗せると指の形になる

`app/globals.css` に一括のルールを追加しました。

原因は Tailwind v4 から、`button` に `cursor: pointer` が自動で付かなくなったことです。個々のボタンに書き足すのではなく、押せる要素（button / リンク / チェックボックス等）すべてにまとめて指定しているので、今後ボタンを増やしても自動で効きます。押せないボタンは「禁止」マークになります。

あわせて、ホバー時にうっすら色が変わる反応も足しました。タブレットにはカーソルがないため、押した瞬間に少し沈む表現（既存の `active:scale`）と両方で反応が伝わります。

---

## 動かし方

```bash
npm install
npm run dev     # http://localhost:3000
npm run build   # 本番ビルド
```

型チェック（`npx tsc --noEmit`）と本番ビルドは通ることを確認済みです。

---

## 追記：GitHub Pages で画像が出なかった件の修正

**症状**：アバター、キャンペーン画像、セール画像、動画のサムネイルがすべて表示されない。

**原因**：Next.js の画像部品（next/image）は、`unoptimized: true` のとき **basePath を自動で付けません**。そのため `/avatars/...` を探しに行き、正しい `/FAKE-CONTACT/avatars/...` に届かず404になっていました。動画（`<video>`）と同じ落とし穴です。

**対処**：`components/game/app-image.tsx` を追加し、画像はすべてこれを通すようにしました。公開先に合わせて住所を直します。

今後、画像を追加するときは **`next/image` を直接使わず、`AppImage` を使ってください。**同じ問題を繰り返さないための決まりです。

```tsx
import { AppImage } from "./app-image"

<AppImage src="/content/新しい画像.png" alt="説明" width={480} height={300} />
```

---

## 追記：動画に声と字幕を追加

「人物がほとんど動かない」「声が出るとよい」というご指摘への対応です。

**声**：端末の読み上げ機能（Web Speech API）で台詞をしゃべらせるようにしました。音声ファイルを持たないので、追加ファイル不要でそのまま公開できます。警察官騙りのときと同じ仕組みです。声は自然に聞こえるもの（七海・圭太・Haruka・Kyoko）を優先して自動で選びます。

**字幕**：しゃべっている内容が画面下に出ます。実際のSNS広告らしくなり、聞き取れない環境でも内容が伝わります。

**台詞の場所**：`lib/scenarios.ts` の `videoSpeech` です。秒数と文章のセットなので、自由に足したり直したりできます。

```ts
videoSpeech: [
  { at: 0.6, text: "皆さん、こんにちは。" },
  { at: 12.4, text: "定員に達し次第締め切ります。今すぐ登録してください。" },
],
```

**映像の動き**：ゆっくりした寄りを大きくしました（1.07倍→1.15倍、横方向の流れも追加）。ただし**元が静止画なので、口元が動くことはありません。**本当に人が動く映像にするには、動画ファイル（mp4）が必要です。

**消音**：再生バー右のスピーカーマークで、声を止められます。

---

## 追記：声の性別を合わせられるようにした

男性の映像なのに女性の声が出ていた件の修正です。声の候補リストで女性名を先に並べていたのが原因でした。

**シナリオごとに話し手の性別を指定できるようにしました。**

```ts
videoVoice: "male",   // または "female"（省略時は女性）
```

端末に男性の声が入っていれば、それを使います（Windows なら圭太・Ichiro、iPad なら Otoya・Hattori）。**入っていない端末では、手持ちの声の高さを下げて男性に近づけます。**iPad は Kyoko しか入っていないことがあるため、その場合の保険です。

完全に自然にはなりませんが、女性の声のまま男性が話すよりは違和感が減るはずです。iPad で聞いてみて、不自然すぎるようなら高さの数値（`lib/speech.ts` の `u.pitch`）を調整できます。
